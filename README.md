# 🦷 Dental Lab Management System

> نظام إداري متكامل للمختبرات السنية تم تطويره باستخدام مجموعة تقنيات **MERN Stack** لضمان بيئة عمل موحدة وفعالة.

---

## 💡 لماذا MERN Stack؟ (MERN Stack Rationale)

تم اختيار MERN Stack (الذي يعتمد بالكامل على لغة JavaScript/JSON) لتوحيد اللغة في جميع طبقات المشروع، مما يوفر بيئة تطوير متجانسة وكفؤة.

### تعريف MERN Stack

| الرمز | التقنية | الوصف |
| :--- | :--- | :--- |
| **M** | **MongoDB** | قاعدة بيانات NoSQL المستخدمة لتخزين بيانات المشروع (الطلبات، الأطباء، السيرفر، إلخ). |
| **E** | **Express.js** | إطار عمل الواجهة الخلفية (Backend Framework). |
| **R** | **React** | مكتبة الواجهة الأمامية (Front End) المستخدمة لبناء واجهات مستخدم تفاعلية. |
| **N** | **Node.js** | بيئة التشغيل التي تقوم بتشغيل Express.js على الخادم، وتوفر ميزة التفاعل اللحظي والكفاءة. |

---

## 🖥️ اللغات والمكتبات المستخدمة (Key Technologies)

### اللغات الأساسية

* **JavaScript:** اللغة الموحدة المستخدمة في كل طبقات المشروع (الواجهة الأمامية/React والخلفية/Node.js).
* **HTML & CSS:** تم استخدامهما لبناء الهيكل وتصميم الواجهة الأمامية (Front End).

### المكتبات والأدوات

| المكتبة/الأداة | الوظيفة | السبب في الاستخدام |
| :--- | :--- | :--- |
| **Mongoose** | التحقق من البيانات (Data Validation) | لضمان صحة البيانات المدخلة قبل تخزينها في MongoDB. |
| **WS (WebSocket)** | الاتصال اللحظي | مكتبة لـ Node.js تستخدم لتطبيق بروتوكول WebSockets، مما يسمح بميزات مثل الدردشة اللحظية. |
| **Bcrypt** | التشفير والأمان | مكتبة تجزئة (Hashing) كلمات المرور لضمان أعلى مستوى من الأمن في المصادقة. |
| **VS Code** | بيئة التطوير | تم استخدامه كبيئة عمل لكتابة الأكواد. |

---

## 📂 هيكلية الملفات الرئيسية

بما أن المشروع يستخدم تقنية MERN، فإنه يتكون من ملفات خادم رئيسية وملفات واجهة أمامية:

* `/server.js`: نقطة الدخول الرئيسية للخادم.
* `/public/`: يحتوي على ملفات الواجهة الأمامية الثابتة (HTML, CSS, JS).
    * `/public/css/`: لملفات التصميم (مثل `admin.css`).
    * `/public/js/`: لملفات JavaScript (مثل `my-orders.js`).
    * `/public/`: لملفات HTML الرئيسية (مثل `contact.html`).

---

## 🚀 كيفية تشغيل المشروع محليًا

1.  **استنساخ المستودع:**
    `git clone https://github.com/fatimadahy716/DentalLab-Management-System.git`
2.  **تنصيب الاعتمادات:**
    `npm install`
3.  **تشغيل الخادم:**
    `node server.js`
4.  **الوصول إلى الواجهة:**
    افتح المتصفح على: `http://localhost:[رقم البورت الذي تستخدمينه في المشروع]`

---

**